[project]
name = "hil-serl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["serl-launcher", "serl-robot-infra"]

[tool.uv.sources]
serl-launcher = { workspace = true, editable = true }
serl-robot-infra = { workspace = true, editable = true }

[tool.uv.workspace]
members = ["serl_launcher", "serl_robot_infra"]
